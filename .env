# Defaults for every build (dev server, plain `npm run build`). The GitHub
# Pages workflow overrides these for the /dev/ deployment via .env.dev-pages.
VITE_APP_LABEL=FinPair
VITE_BASE_PATH=/Finance-Pocket-App/
VITE_DB_NAME=finance-pocket
VITE_DEV_BUILD=0
